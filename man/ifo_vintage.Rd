% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifo.R
\name{ifo_vintage}
\alias{ifo_vintage}
\title{Return ifo business climate vintage data}
\source{
\url{https://www.ifo.de/en/ifo-time-series}
}
\usage{
ifo_vintage(
  type = c("germany", "industry", "manufacturing", "services", "trade", "wholesale",
    "retail", "construction")
)
}
\arguments{
\item{type}{(\code{character(1)})\cr
Defaults to \code{"germany"}. One of:
\itemize{
\item \code{"germany"}: returns the vintages of the ifo business climate index for Germany.
\item \code{"industry"}, \code{"manufacturing"}, \code{"services"}, \code{"trade"}, \code{"wholesale"}, \code{"retail"},
\code{"construction"}: returns the vintages of the ifo business climate for the sector.
}}
}
\value{
A \code{data.frame()} containing the monthly ifo business climate vintages in long format.
}
\description{
Return ifo business climate vintage data
}
\details{
A vintage is the time series as published in a given month. Each vintage runs from the start
of the series to its own release month, so later vintages contain more months and may revise
earlier values, for example through seasonal adjustment. The output contains one observation
per row in \code{value}. The other columns describe each observation:
\itemize{
\item \code{yearmonth}: the observed month.
\item \code{vintage}: the first day of the month in which the series was published.
\item \code{indicator}: climate, situation, or expectation.
\item \code{series}: index or balance.
}

\code{type = "germany"} and \code{type = "industry"} return an index with 2015 = 100. All other sectors
return balances. \code{type = "industry"} corresponds to "Industry and Trade" in the source.
ifo updates the vintage workbooks less often than the monthly releases, so the latest vintage
can lag the current \code{\link[=ifo_business]{ifo_business()}} release by several months.
}
\examples{
\dontshow{if (curl::has_internet()) withAutoprint(\{ # examplesIf}
\donttest{
vintage <- ifo_vintage("germany")
head(vintage)
}
\dontshow{\}) # examplesIf}
}
